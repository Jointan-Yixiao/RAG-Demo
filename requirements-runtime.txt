# Runtime dependencies for the E41 release (Python 3.12, verified 3.12.14).
# Install torch/torchvision FIRST from the PyTorch CUDA 12.8 index (see README),
# then:  python -m pip install -r requirements-runtime-lock.txt
# This file lists the direct dependencies; the lock pins the full closure.
# Not included on purpose: sentence-transformers (not imported by the runtime;
# its current releases require huggingface-hub>=1.3, incompatible with
# transformers 4.51.3), scikit-learn/scipy (experiment-only).
#
# torch==2.11.0+cu128        (separate step, PyTorch index)
# torchvision==0.26.0+cu128  (separate step, PyTorch index)
transformers==4.51.3          # GME remote code needs AutoModelForVision2Seq (removed in 5.x)
tokenizers==0.21.4
huggingface_hub==0.36.2
accelerate==1.14.0            # device_map for GME
safetensors==0.8.0
qwen-vl-utils==0.0.14
numpy==2.5.2
pillow==12.3.0
jsonschema==4.26.0
requests==2.34.2
tqdm==4.70.0
